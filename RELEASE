# pyFormex version
VERSION=1.0.3
# version for public documentation directory
DOCVERSION=1.0
# Unless you are creating an official release, a tail should be added
# to the ${VERSION}, starting with -a   !!!!!!!!!!!!!!!
#
# NOTE: we do no longer use the ~ character, for sorting reasons
# We should not create Debian packages for -a versions
#
RELEASE=${VERSION}-a3

